"""LangGraph orchestration.

graph.py  — assembles the agents into the pipeline graph (compiled as `app`)
nodes.py  — binds each agent into a graph node over the shared state
state.py  — the PipelineState carried between nodes
"""
